REST Client.
Rest client is Web application which can be started by mvn jetty:run from airportmanager-rest_client directory. Client is then avaialable on http://localhost:9090/restclient/. For proper functionality, REST server must be running. REST server is running together with Web interface (/airportmanager-web). Path to rest server can be set in Settings tab of rest client.